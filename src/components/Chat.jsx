export default function(cons){

    function checkerChat(params) {

        return console.log("{params}");
        <p>im sorry to myself, this is gonna chamge from now, i promise</p>
        
    }

    return <div><p>....</p></div>
}